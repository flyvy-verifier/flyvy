[package]
name = "formats"
version.workspace = true
edition.workspace = true

[dependencies]
fly = { path = "../fly" }
smtlib = { path = "../smtlib" }
solver = { path = "../solver" }

itertools = "0.10.5"
